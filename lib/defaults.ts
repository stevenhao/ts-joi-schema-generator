// Default suffix appended to generated files.
export const fileSuffix = '-schema';

// Default suffix appended to generated files.
export const schemaSuffix = 'Schema';

// Default render type
export const render = 'joi';